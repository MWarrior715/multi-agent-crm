# Multi-Agent CRM Assistant — dependencias
# Motor de IA: cliente OpenAI-compatible (habla con cualquier endpoint /v1, local o cloud).
openai>=1.35.0

# Persistencia + modelos
sqlmodel>=0.0.21
pydantic>=2.7.0

# Interfaz
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
streamlit>=1.36.0

# Config
python-dotenv>=1.0.1

# Tests
pytest>=8.2.0
